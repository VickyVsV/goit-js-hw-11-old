name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3  # обновлённая версия

      - name: Install dependencies and build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4  # последняя стабильная версия
        with:
          branch: gh-pages      # ветка для деплоя
          folder: dist          # папка со сборкой
